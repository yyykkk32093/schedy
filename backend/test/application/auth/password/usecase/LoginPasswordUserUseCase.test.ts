import { describe, it } from 'vitest'

/**
 * このファイルは legacy（旧 LoginPasswordUserUseCase）由来。
 * 現行は ApplicationEvent ベースの SignInPasswordUserUseCase に移行済みのため、
 * テストは必要になったタイミングで復活させる。
 */
describe('LoginPasswordUserUseCase (legacy)', () => {
    it.todo('SignInPasswordUserUseCase に合わせてテストを再設計する')
})
// // })
