# Phase 4 — チャットスレッド機能

> **最終更新**: 2026-03-16
> **ステータス**: ❌ 未着手

## フェーズ概要
- **ゴール**: チャットにスレッド（返信）機能を実装する
- **対象**: W3-15
- **変更対象レイヤー**: UI / API / UseCase
- **規模**: L〜XL（DB準備済み。UIの大規模新規実装が中心）

## タスク一覧

| タスク                                                       | 状態     | 備考 |
| ------------------------------------------------------------ | -------- | ---- |
| W3-15 チャット: スレッド機能（parentMessageId活用 + UI）     | ❌ 未着手 |      |

---

## W3-15 チャット: スレッド機能

- **分類**: 新機能
- **優先度**: P2
- **変更対象**: 両方
- **変更レイヤー**: UI / API / UseCase
- **由来**: wave2繰越 #25
- **依存**: wave2 Phase3 のチャット機能改善（#18〜#21: リアクション・削除済み表示等）が完了していること

### 前提
- DB: `Message.parentMessageId`（自己参照FK）は wave2 で追加済み。追加のDB変更は不要
- wave2 で実装済みの機能: 絵文字リアクション、論理削除、削除確認ダイアログ、送信者名表示

### 仕様

#### メッセージ一覧（メインビュー）
| 項目 | 内容 |
|------|------|
| スレッドカウント表示 | メッセージバブルの下部に「💬 N件の返信」リンクを表示（parentMessageId = このメッセージID の件数） |
| 最新返信プレビュー | スレッドカウントの横に最新返信の送信者名 + 本文（1行トランケート）を表示 |
| スレッドへの遷移 | 「N件の返信」リンクをタップでスレッドビューを開く |

#### スレッドビュー
| 項目 | 内容 |
|------|------|
| 表示形式 | スライドインパネル（モバイル: フルスクリーンモーダル） |
| ヘッダー | 親メッセージの送信者名 + 本文プレビュー + 閉じるボタン |
| 本文 | 親メッセージ（先頭固定表示） + 返信一覧（時系列昇順） |
| 入力欄 | 通常のメッセージ入力と同様（テキスト + 画像添付） |
| リアクション | 返信メッセージにもリアクション可能 |
| ネスト制限 | **1階層のみ**（返信への返信はフラットに同一スレッド内に表示） |

#### バックエンドAPI
| エンドポイント | 動作 |
|---------------|------|
| `POST /channels/:channelId/messages` | parentMessageId を指定で返信作成 |
| `GET /channels/:channelId/messages/:messageId/thread` | スレッド返信一覧取得（ページネーション対応） |
| メッセージ一覧取得（既存） | レスポンスに `replyCount` フィールドを追加 |

### 受入条件
- メッセージに対してスレッド（返信）を作成できること
- メッセージバブル下部に「N件の返信」が表示されること
- タップでスレッドビューが開き、返信一覧が表示されること
- スレッドビュー内で返信の送受信ができること
- 返信への返信はフラットに表示されること（ネスト不可）
- リアルタイム更新（WebSocket / ポーリング）でスレッドの返信件数が更新されること

### 実装ステップ（推奨順序）
1. **バックエンドAPI**: スレッド取得API新設 + メッセージ一覧に replyCount 追加
2. **メインビュー**: MessageBubble にスレッドカウント + 最新返信プレビュー表示
3. **スレッドビュー**: ThreadView コンポーネント新設（スライドインパネル）
4. **入力・送信**: スレッド内メッセージ送信（parentMessageId 指定）
5. **リアルタイム**: 既存のWebSocket/ポーリング機構にスレッド更新を追加

### 関連ファイル（推定）
- `frontend/src/features/chat/` — ThreadView（新規）、MessageBubble 拡張
- `frontend/src/features/chat/components/MessageBubble.tsx` — スレッドカウント表示追加
- `backend/src/api/front/chat/` — スレッドAPI新設
- `backend/src/application/chat/` — スレッド取得UseCase

---

## 作業ログ
- 2026-03-16: Phase 4 作成。チャットスレッド機能を計画。
