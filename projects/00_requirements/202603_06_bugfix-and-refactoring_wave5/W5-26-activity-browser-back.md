# W5-26 アクティビティ画面 — 作成/更新後ブラウザバック修正

> **最終更新**: 2026-04-14
> **ステータス**: ✅ 完了（既に実装済み）

## 概要
- **ゴール**: アクティビティ作成/更新後のブラウザバックでフォーム画面に戻らないようにする
- **対象**: フロントエンド（ナビゲーション）
- **由来**: wave4 carry-over [12][13]

---

## タスク一覧

| #   | タスク                                                 | 状態   | carry-over # | 備考                              |
| --- | ------------------------------------------------------ | ------ | ------------ | --------------------------------- |
| 1   | アクティビティ作成後 → 詳細画面遷移を `replace` に変更 | ✅ 完了 | [12]         | 既に `{ replace: true }` 適用済み |
| 2   | アクティビティ更新後 → 詳細画面遷移を `replace` に変更 | ✅ 完了 | [13]         | 既に `{ replace: true }` 適用済み |

---

## 実装メモ
- W5-21 [1]（コミュニティ作成後のブラウザバック）と同じパターン
- 作成/更新完了後の `navigate('/activities/' + id)` を `navigate('/activities/' + id, { replace: true })` に変更
- これでヒストリスタックからフォーム画面が除外され、ブラウザバックでフォームに戻らない
- 対象ファイル:
  - アクティビティ作成ページ（ActivityCreatePage 等）
  - アクティビティ更新ページ（ActivityEditPage 等）

---

## 作業ログ
- 2026-04-12: タスク分解ファイル作成
- 2026-04-14: Phase 2 コード調査にて既に `{ replace: true }` が両ページに適用済みと判明。完了扱い。
